# For local dev you can leave empty because Vite proxy will forward /api -> backend
# For production deployments set this to your backend base URL (no trailing slash)
# Example: https://your-backend-domain.com
VITE_API_URL=
