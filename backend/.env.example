# Backend environment variables

# Gemini / LLM
GEMINI_API_KEY=

# Auth token signing secret (change in production)
AUTH_TOKEN_SECRET=dev-secret-change-me

# CORS (optional)
# CORS_ALLOW_ORIGINS=http://localhost:3000
