# Shared environment variables (used mainly by backend)
# IMPORTANT: Create a real .env (not committed) before running.

# Gemini / LLM
GEMINI_API_KEY=

# Auth token signing secret (change in production)
AUTH_TOKEN_SECRET=dev-secret-change-me
